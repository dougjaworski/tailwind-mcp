# Tailwind CSS MCP Server Configuration
# Copy this file to .env and customize for your environment

# Server Configuration
# Port the MCP server listens on
MCP_PORT=8000

# Host the MCP server binds to (0.0.0.0 allows external connections)
MCP_HOST=0.0.0.0

# Allowed Hosts for DNS Rebinding Protection
# Comma-separated list of hostnames that are allowed to connect
# Use * as wildcard for ports (e.g., localhost:*)
# For local-only access: localhost:*,127.0.0.1:*
# For Docker access from host: Add 0.0.0.0:*
# For remote access: Add your server hostname(s)
MCP_ALLOWED_HOSTS=localhost:*,127.0.0.1:*,0.0.0.0:*

# Data Directory
# Path where documentation and database will be stored
DATA_DIR=/app/data

# Logging Level
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO
